# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp

# Build artifacts (for local bin/ builds)
target/

# Per-platform binaries dispatched to by bin/<name> wrappers.
# Wrappers are tracked; platform binaries are produced by builds/releases.
bin/*-linux-*
bin/*-darwin-*
bin/*-windows-*.exe
